---
import Search from './Search.astro';

const navLinks = [
  { label: '~/artigos', href: '/posts' },
  { label: '~/materiais', href: '/materiais' },
  { label: '~/canal', href: 'https://youtube.com/ROVEEb', external: true },
  { label: '~/sobre', href: '/sobre' },
];

const currentPath = Astro.url.pathname;
const isActive = (href: string) => {
  if (href.startsWith('http')) return false;
  if (href === '/') return currentPath === '/';
  return currentPath === href || currentPath.startsWith(href + '/');
};
---

<header class="site-header">
  <div class="header-inner">
    <a href="/" class="logo" aria-label="Roveda, página inicial">
      <!-- Light: simbolo-claro (R navio #061736 em fundo claro) -->
      <svg viewBox="0 0 100 100" width="34" height="34" aria-hidden="true" class="logo-mark mark-light">
        <path d="M32 86 V14 H54 A17 17 0 0 1 54 48 H32 M52 48 L72 86" fill="none" stroke="#061736" stroke-width="10" stroke-linecap="round" stroke-linejoin="round"></path>
        <circle cx="71" cy="31" r="7.5" fill="#061736"></circle>
        <circle cx="32" cy="86" r="8" fill="#FCFFFE" stroke="#061736" stroke-width="5"></circle>
        <circle cx="72" cy="86" r="8" fill="#FCFFFE" stroke="#077E7D" stroke-width="5"></circle>
      </svg>
      <!-- Dark: simbolo-navio (R mint #92FDEF em fundo navio #061736) -->
      <svg viewBox="0 0 100 100" width="34" height="34" aria-hidden="true" class="logo-mark mark-dark">
        <path d="M32 86 V14 H54 A17 17 0 0 1 54 48 H32 M52 48 L72 86" fill="none" stroke="#92FDEF" stroke-width="10" stroke-linecap="round" stroke-linejoin="round"></path>
        <circle cx="71" cy="31" r="7.5" fill="#92FDEF"></circle>
        <circle cx="32" cy="86" r="8" fill="#061736" stroke="#92FDEF" stroke-width="5"></circle>
        <circle cx="72" cy="86" r="8" fill="#061736" stroke="#EFFEFC" stroke-width="5"></circle>
      </svg>
      <span class="logo-type">Roveda</span>
      <span class="logo-tagline">liberdade de conhecimentos</span>
    </a>

    <nav class="nav" aria-label="Navegação principal">
      <ul>
        {navLinks.map(({ label, href, external }) => (
          <li>
            <a
              href={href}
              target={external ? '_blank' : undefined}
              rel={external ? 'noopener noreferrer' : undefined}
              class:list={['nav-link', { active: isActive(href) }]}
            >
              {label}{external ? ' ↗' : ''}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <div class="header-actions">
      <a href="mailto:contato@roveeb.com" class="contact-btn">contato</a>
      <Search />
      <button id="theme-toggle" type="button" class="theme-toggle" aria-label="Alternar tema claro e escuro" title="Alternar tema">
        <svg class="icon-moon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
        </svg>
        <svg class="icon-sun" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <circle cx="12" cy="12" r="5"></circle>
          <line x1="12" y1="1" x2="12" y2="3"></line>
          <line x1="12" y1="21" x2="12" y2="23"></line>
          <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
          <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
          <line x1="1" y1="12" x2="3" y2="12"></line>
          <line x1="21" y1="12" x2="23" y2="12"></line>
          <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
          <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
        </svg>
      </button>
    </div>
  </div>
</header>

<style>
  .site-header {
    background: var(--surface-page);
    border-bottom: 1px solid var(--border-subtle);
    position: sticky;
    top: 0;
    z-index: 100;
  }
  :global(html[data-theme='dark']) .site-header {
    background: #061736;
    border-bottom-color: rgba(146, 253, 239, 0.12);
  }

  .header-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 32px;
    height: 76px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 28px;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    flex-shrink: 0;
  }

  .logo-mark {
    width: 34px;
    height: 34px;
    flex: none;
  }
  .mark-dark { display: none; }
  :global(html[data-theme='dark']) .mark-light { display: none; }
  :global(html[data-theme='dark']) .mark-dark { display: block; }

  .logo-type {
    font-family: 'Poppins', sans-serif;
    font-weight: 700;
    font-size: 20px;
    letter-spacing: -0.02em;
    color: #061736;
    line-height: 1;
  }
  :global(html[data-theme='dark']) .logo-type {
    color: #EFFEFC;
  }

  .logo-tagline {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    letter-spacing: 0.14em;
    text-transform: lowercase;
    color: #014144;
    margin-left: 2px;
    line-height: 1;
    display: block;
    /* hide on narrow, show on wide */
  }
  :global(html[data-theme='dark']) .logo-tagline {
    color: #92FDEF;
    opacity: 0.85;
  }
  @media (max-width: 1100px) {
    .logo-tagline { display: none; }
  }

  .nav ul {
    list-style: none;
    display: flex;
    align-items: center;
    gap: 28px;
  }

  .nav-link {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--text-muted);
    text-decoration: none;
    padding-bottom: 2px;
    border-bottom: 2px solid transparent;
    transition: color 0.15s, border-color 0.15s;
  }
  :global(html[data-theme='dark']) .nav-link {
    color: rgba(239, 254, 252, 0.72);
  }
  .nav-link:hover,
  .nav-link.active {
    color: var(--text-strong);
    border-bottom-color: var(--accent-highlight);
    text-decoration: none;
  }
  :global(html[data-theme='dark']) .nav-link:hover,
  :global(html[data-theme='dark']) .nav-link.active {
    color: #EFFEFC;
    border-bottom-color: #92FDEF;
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: 14px;
    flex-shrink: 0;
  }

  .contact-btn {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--text-strong);
    background: var(--surface-raised);
    border: 1px solid var(--border-subtle);
    border-radius: 7px;
    padding: 8px 14px;
    text-decoration: none;
    transition: background 0.15s, border-color 0.15s;
    white-space: nowrap;
  }
  :global(html[data-theme='dark']) .contact-btn {
    color: #EFFEFC;
    background: rgba(146, 253, 239, 0.08);
    border-color: rgba(146, 253, 239, 0.18);
  }
  .contact-btn:hover {
    background: var(--accent-highlight);
    border-color: var(--text-strong);
    color: var(--text-strong);
    text-decoration: none;
  }
  :global(html[data-theme='dark']) .contact-btn:hover {
    background: #92FDEF;
    color: #061736;
    border-color: #92FDEF;
  }

  .theme-toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    padding: 0;
    background: var(--surface-raised);
    border: 1px solid var(--border-subtle);
    border-radius: 7px;
    color: var(--text-muted);
    cursor: pointer;
    transition: border-color 0.15s, color 0.15s, background 0.15s;
    flex-shrink: 0;
  }
  :global(html[data-theme='dark']) .theme-toggle {
    background: rgba(146, 253, 239, 0.08);
    border-color: rgba(146, 253, 239, 0.14);
    color: rgba(239,254,252,0.7);
  }
  .theme-toggle:hover {
    border-color: var(--text-strong);
    color: var(--text-strong);
  }
  :global(html[data-theme='dark']) .theme-toggle:hover {
    border-color: #92FDEF;
    color: #92FDEF;
  }

  .theme-toggle svg { display: block; }
  .theme-toggle .icon-sun { display: none; }
  :global(html[data-theme='dark']) .theme-toggle .icon-moon { display: none; }
  :global(html[data-theme='dark']) .theme-toggle .icon-sun { display: block; }

  @media (max-width: 860px) {
    .header-inner {
      padding: 0 20px;
      gap: 16px;
      height: auto;
      min-height: 64px;
      flex-wrap: wrap;
      padding-top: 12px;
      padding-bottom: 12px;
    }
    .nav { order: 3; flex-basis: 100%; }
    .nav ul { gap: 18px; flex-wrap: wrap; }
  }

  @media (max-width: 520px) {
    .header-inner { padding: 12px 16px; }
    .nav ul { justify-content: space-between; width: 100%; }
    .contact-btn { padding: 6px 10px; font-size: 12px; }
  }
</style>

<script is:inline>
  (function () {
    const btn = document.getElementById('theme-toggle');
    if (!btn) return;
    btn.addEventListener('click', function () {
      const root = document.documentElement;
      const next = root.dataset.theme === 'dark' ? 'light' : 'dark';
      root.dataset.theme = next;
      root.style.setProperty('color-scheme', next);
      try { localStorage.setItem('theme', next); } catch (e) {}
      const meta = document.querySelector('meta[name="theme-color"]');
      if (meta) meta.setAttribute('content', next === 'dark' ? '#061736' : '#FCFFFE');
    });
  })();
</script>
